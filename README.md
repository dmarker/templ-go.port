# templ-go.port

A FreeBSD port.

This is a pretty trivial go module to have in your projects but it does
build to a stand alone utility as well.

```
# cd /usr/ports/devel
# git clone git@github.com:dmarker/templ-go.port.git templ-go
# cd templ-go
# make install clean
```
